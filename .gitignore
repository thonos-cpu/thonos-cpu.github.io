# Secrets / environment — NEVER commit (covers .env and .env.example)
.env
.env.*
*.env
secrets.*
*.pem
*.key
*.p12
*.pfx

# Build output
dist/
.astro/

# Dependencies
node_modules/

# Build-time data cache (regenerated on every build)
.cache/

# Local agent / tooling (not part of the site)
.claude*
.agents*
.impeccable
skills-lock.json

# Editor / OS noise
.vscode/
.idea/
.DS_Store
Thumbs.db
*.log
npm-debug.log*
